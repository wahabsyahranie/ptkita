import 'package:flutter/material.dart';

class WarrantyHistoryPage extends StatelessWidget {
  const WarrantyHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Riwayat Garansi')));
  }
}
